services:
  - type: web
    name: butler-chat
    runtime: node
    plan: starter
    region: oregon
    buildCommand: npm install && npm run build
    startCommand: npm run start
    healthCheckPath: /
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        fromDatabase:
          name: butler-db
          property: connectionString

databases:
  - name: butler-db
    plan: basic-256mb
    diskSizeGB: 1
    region: oregon
    databaseName: butler
    user: butler
